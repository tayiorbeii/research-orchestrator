# Dependencies
node_modules/

# Build output (regenerated by `npm run build`)
dist/

# Runtime research artifacts (per-run output + cache)
.research/
.research-cache/

# Personal/private material — never include in product commits
research/
.pi/
.pi-persona/
phase0-defy-thumb-meter/
plans/
*.traineddata
*.zip

# Ephemeral agent/pipeline artifacts (pi-subagents transcripts, etc.)
.pi-subagents/

# Pipeline output (per-video screenshots, crops, manifests, prompts)
out/

# TypeScript incremental build info
*.tsbuildinfo

# Real environment files — NEVER commit. Use .env.example as the template.
.env
.env.local
.env.*.local
.env.production
.env.development

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editors
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
